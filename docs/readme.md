## Supporting Documents

The `docs/city_costs.xlsx` file contains sample cost data for major cities.

This dataset is used as a fallback source for budget estimation when external APIs are unavailable. It includes estimated daily costs for:

- Hotel
- Food
- Local Transport
- Activities
